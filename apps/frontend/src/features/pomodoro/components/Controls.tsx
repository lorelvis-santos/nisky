import { CheckCircle2, Pause, Play, Square } from "lucide-react";

export function Controls({ running, paused, onStart, onPause, onResume, onStop, onComplete }: { running: boolean; paused: boolean; onStart: () => void; onPause: () => void; onResume: () => void; onStop: () => void; onComplete: () => void }) {
  if (!running) return <button className="flex w-full max-w-md items-center justify-center gap-2 bg-primary-container px-6 py-4 font-body-md text-body-md text-on-primary hover:bg-primary" onClick={onStart} type="button"><Play size={18} /> Iniciar Pomodoro</button>;
  return <div className="flex w-full max-w-md flex-wrap items-center gap-3"><button className="flex min-w-[130px] flex-1 items-center justify-center gap-2 border border-outline-variant bg-surface-container-lowest px-5 py-4 font-body-md text-body-md text-on-surface-variant hover:bg-surface-container-high" onClick={paused ? onResume : onPause} type="button">{paused ? <Play size={18} /> : <Pause size={18} />} {paused ? "Reanudar" : "Pausar"}</button><button className="flex min-w-[130px] flex-1 items-center justify-center gap-2 border border-outline-variant bg-surface-container-lowest px-5 py-4 font-body-md text-body-md text-on-surface-variant hover:bg-surface-container-high" onClick={onStop} type="button"><Square size={18} /> Detener</button><button className="flex w-full items-center justify-center gap-2 bg-primary-container px-5 py-4 font-body-md text-body-md text-on-primary hover:bg-primary" onClick={onComplete} type="button"><CheckCircle2 size={18} /> Marcar como completada</button></div>;
}
